<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.h3c.itrd.core.mapper.DesignHardwareScoreMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.h3c.itrd.core.entity.DesignHardwareScoreDO">
        <id column="id" property="id" />
        <result column="account" property="account" />
        <result column="quarter" property="quarter" />
        <result column="example_num" property="exampleNum" />
        <result column="tool_num" property="toolNum" />
        <result column="store_num" property="storeNum" />
        <result column="example_score" property="exampleScore" />
        <result column="tool_score" property="toolScore" />
        <result column="store_score" property="storeScore" />
        <result column="total_score" property="totalScore" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, account, quarter, example_num, tool_num, store_num, example_score, tool_score, store_score, total_score
    </sql>

</mapper>
